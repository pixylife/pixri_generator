  Future<List<{{.Model.Name}}>> getListOf{{.Model}}() async {
    final response = await client.get("$baseUrl{{.API.URL}}");
    if (response.statusCode == 200) {
      return {{.Model.Name}}FromJson(response.body);
    } else {
      return null;
    }
  }